import { ServiceFileBuilder } from '../types';

// find the operation
// normalize operation (print?)
// gen hash for normalized operation
// embed hash

export const fileBuilder: ServiceFileBuilder = (
  schema,
  documents,
  config,
  info,
) => {
  return '# service file content';
};
